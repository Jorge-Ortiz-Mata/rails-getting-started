<div class="bg-white">
  <div class="flex flex-col h-screen border">
    <%= render 'shared/navbar' %>

    <div class="flex flex-col justify-center items-center sm:px-6 lg:px-8 h-full gap-10">
      <div class="flex flex-col">
        <h2 class="mt-6 text-center text-2xl font-bold leading-9 tracking-tight text-gray-900">Reenviar correo de confirmación</h2>
        <p class=" text-center text-sm sm:text-base">
          Ingresa tu correo electrónico para que recibas el correo de confirmación en dado caso
          de que no hayas recibido el link.
        </p>
      </div>

      <%= form_with(model: @confirmation, url: confirmation_send_path, method: :patch, class: 'w-full md:w-7/12 flex flex-col gap-5', data: { turbo: false }) do |f| %>
        <div class="flex flex-col w-full items-start gap-1 mb-5">
          <%= f.label :email, 'Correo electrónico:', class: 'label-style ' %>
          <%= f.email_field :email, class: "input-style #{error_border(@confirmation.errors.include?(:email))}" %>
          <% if @confirmation.errors.include?(:email) %>
            <%= render 'shared/error_input', { object: @confirmation, attribute: :email } %>
          <% end %>
        </div>

        <div class="flex flex-col w-full items-center mt-10">
          <%= f.submit "Enviar correo de confirmación", class: 'flex w-full justify-center rounded-md bg-red-600 px-3 py-1.5 text-sm font-semibold leading-6 text-white shadow-sm hover:bg-red-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-red-600' %>
        </div>
      <% end %>
    </div>
  </div>
</div>
